---
name: wf:plan
description: Create a structured implementation plan from a description or spec file
argument-hint: "<description or spec-file>"
---

# IMPORTANT: This command MUST invoke the skill `wf-plan`

The ONLY purpose of this command is to call the `wf-plan` skill. You MUST use that skill now.

**Input:** $ARGUMENTS

Pass the input to the skill. The skill handles the planning logic.
