---
name: wf
description: Show workflow status and available commands
argument-hint: ""
---

# IMPORTANT: This command MUST invoke the skill `wf`

The ONLY purpose of this command is to call the `wf` skill. You MUST use that skill now.

**User request:** $ARGUMENTS

Pass the request to the skill. The skill handles the status display logic.
