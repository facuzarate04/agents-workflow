---
name: wf:interview
description: Gather requirements through focused questions to create a spec file
argument-hint: "<topic>"
---

# IMPORTANT: This command MUST invoke the skill `wf-interview`

The ONLY purpose of this command is to call the `wf-interview` skill. You MUST use that skill now.

**Topic:** $ARGUMENTS

Pass the topic to the skill. The skill handles the interview logic.
